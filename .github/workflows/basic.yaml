name: Basic workflow

on:
  push

jobs:
  basic-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: 'echo "Hello world"'

      - name: Hello World
        run: 'echo ${{ os.runner }}'
